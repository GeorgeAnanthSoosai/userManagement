import { AgeClasificationPipe } from './age-clasification.pipe';

describe('AgeClasificationPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeClasificationPipe();
    expect(pipe).toBeTruthy();
  });
});
